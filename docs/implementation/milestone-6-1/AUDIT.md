# M6.1 fresh audit — 2026-09-09

Starting local SHA: `9b33b64357fa5c1e6c99d0868c7dcf6f4af55dcd`.
Dedicated worktree `m6-1-sanctuary`, branch `milestone-6-1-sanctuary-refinement`.
The original dirty `site` checkout and all older worktrees are preserved.

Before runtime edits: production build, 82/82 Node tests, approved-motion and
default-animal parity passed. Three remains 0.175.0. Fresh Brave NVIDIA WebGL2
captures inspected the public site and local Drift/Documentary/Intimate/Deep,
Explore and 390×844 portrait. Public main remained `a2ed3bc89c700ba5c792ecac19747939e9f09b8d`,
not the local M6 baseline. No publication is authorized.

Evidence root: `/home/mani/dev/jellyfish-studio/m6-1-evidence/`.
Fresh source sets: `public-audit`, `audit-modes`, `audit-portrait`,
`detail-before`, `plume-before-motion`, `detail-matched-before`.

## Findings and sequence

1. The plume uses 144 very large soft alpha quads; grey, mostly uniform rounded
   envelopes obscure internal mineral structure. Independent seeded phases also
   undermine shared turbulent domains. Replace this construction first; do not
   change minerals until browser motion demonstrates granular discharge.
2. M3 owns a live RGBA16F color/depth target but bypasses it outside active bubble
   slots. Merely reading that texture at the vent would be stale. Reuse the same
   capture/output path while a bounded vent optical region is visible, never a
   second ocean render or separate post stack.
3. Mineral noise frequencies currently compete at similar strength across the
   whole surface. They do not explain *where* pale deposits or oxidation belong.
   Make orientation/source/height/cavity proxies dominant and scanned roughness
   secondary, preserving the one pooled animal illumination.
4. Portrait loses the defining main spire along the left edge. Prefer one small
   world-space placement adjustment, not a new camera or per-viewport world.

Fresh scientific reference: NOAA's [small black smoker](https://oceanexplorer.noaa.gov/multimedia/okeanos-explorations-22voyage-to-the-ridge-gallery-media-ex2205-dive02-smoker/)
video downloaded and sampled at nine seconds in `references/`. It separates
transparent refractive discharge, granular dark surfaces and localized pale /
oxidized crust. Its ROV-lit brightness is **not** the natural-light target for
this dark artwork. NOAA's [PMEL FAQ](https://www.pmel.noaa.gov/eoi/FAQ.html) also
distinguishes focused mineral discharge from clearer diffuse flow. No external
artwork or implementation copied.

## Constraints retained

No animal, camera, View, scrolling, current-field, population, idle or public
release edits. The pre-existing cyan-tissue loss after idle return is explicitly
out of scope and remains documented, not silently repaired.
