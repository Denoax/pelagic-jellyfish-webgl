// One-time salvage transform. README.md is the editable publication after this pass.
// Existing manuscript remains a content cache; this does not build a paper website.
import {readFileSync,writeFileSync} from 'node:fs';
import {execFileSync} from 'node:child_process';
const read=f=>readFileSync(f,'utf8');
const manuscript=read('paper/manuscript.md'),old=execFileSync('git',['show','2bea550:README.md'],{encoding:'utf8'});
const runtime='bce3571b0300ecfe5dc6e5dd45f28a9cda57006e';
const blob=`https://github.com/Denoax/pelagic-jellyfish-webgl/blob/${runtime}/`;
const sec=(text,title)=>{const start=text.indexOf('# '+title+'\n');if(start<0)throw Error(title);const from=start+title.length+3,next=text.indexOf('\n# ',from);return text.slice(from,next<0?undefined:next).trim();};
const m=title=>sec(manuscript,title);
const os=title=>{const start=old.indexOf('## '+title+'\n');const from=start+title.length+4,next=old.indexOf('\n## ',from);return old.slice(from,next<0?undefined:next).trim();};
const link=(path,lines='')=>`[${path.split('/').at(-1)}](${blob}${path}${lines?'#L'+lines:''})`;
const source=(...paths)=>'**Source:** '+paths.map(p=>Array.isArray(p)?link(...p):link(p)).join(' · ')+'.';
const img=(file,alt)=>`![${alt}](${file})`;
const section=(title,...body)=>'## '+title+'\n\n'+body.filter(Boolean).join('\n\n');
const refLabels={costello2021:'Costello et al. (2021)',gemmell2013:'Gemmell et al. (2013)',muller2007:'Müller et al. (2007)',blinn1982:'Blinn (1982)',wyman2005:'Wyman (2005)',aurelia:'Aurelia',three175:'Three.js r175',fluidglass:'FluidGlass',polyhaven:'Poly Haven Rock 07'};
const graph=old.match(/```mermaid[\s\S]*?```/)[0];
const headings=['Technical overview','Mathematical notation','1. Procedural jellyfish geometry','2. Bell deformation, rowing cycle, and locomotion','3. Soft-body appendage dynamics','4. Tissue optics and bioluminescence','5. Connected ocean current and wake field','6. Population and screen-space LOD','7. Camera as a one-parameter journey','8. Live refraction and bubble passage','9. Abyssal hydrothermal sanctuary','10. Ocean-to-glass liquid system','Numerical robustness','Rendering architecture','Engineering ablations / failed approaches','Performance','Validation','Reproducibility','Limitations','Technical stack','Repository map','References','Citation','Licensing and asset provenance'];
const slug=s=>s.toLowerCase().replace(/[^\p{L}\p{N}_\- ]/gu,'').replace(/ /g,'-');
const start=`<div align="center">\n\n# Pelagic\n\n**Real-Time Procedural Jellyfish, Soft-Body Animation, Underwater Optics, and Interactive Liquid Glass in Three.js**\n\n**Mani Marami Milani**\n\n[Live Demo](https://denoax.github.io/pelagic-jellyfish-webgl/) · [Technical Overview](#technical-overview) · [Reproduce](#reproducibility) · [Cite](#citation)\n\n</div>\n\n${img('docs/readme/hero.gif','Actual browser montage: luminous jellyfish, travelling midwater, dark hydrothermal sanctuary, and the live-ocean liquid clock.')}\n\n*Four excerpts from the existing approved-runtime recordings, edited in that order; not one continuous camera take. All motion on this page is actual browser output. Diagrams are labeled separately.*`;
const overview=m('1. Introduction').replace(/Figure 1 introduces the\nruntime; the motion supplement separates the behaviors that a still cannot prove\./,'The inline motion previews separate behaviors that a still cannot prove.').replace(/!\[Figure 1\.[\s\S]*?\)\n?$/,'').replace('supplemental negative evidence','historical negative evidence below');
let notation=m('5. Notation and spaces').replace('The complete symbol table is in `notation.md`.','The compact table below summarizes recurring symbols; each method defines its local variables.').replace('`source-map.md` records immutable paths and line ranges for every equation.','Pinned source links accompany each method; the retained [source map](paper/source-map.md) records all 22 equation groups.');
notation+='\n\n'+read('paper/notation.md').split('\n\n')[1];
notation+='\n\nLocal letters are reused between methods, not implicitly shared state: appendage $x_i^-$ is a previous position; optical $D$ may be a ray; idle $D$ is displacement. A superscript $+$ or prime means the next update, not a derivative. $S_{a,b}(x)=S((x-a)/(b-a))$ uses the same clamped cubic smoothstep. Numerical coefficients carry the artistic scale of their stated coordinates; they are not SI material constants.';
let bell=m('7. Bell kinematics').replace('S1 supplies the corresponding motion;\nS1 shows','the retained S1 master shows').replace('S1 supplies continuous motion.','The retained S1 master supplies continuous motion.');
bell=bell.replace('$T_1=W(q;0.025,0.215)$, secondary thrust $T_2=W(q;0.57,0.82)$, and margin roll\n$m_r=0.78T_1-0.52W(q;0.22,0.66)$.', () => '$T_1$, secondary thrust $T_2$, and margin roll $m_r$:\n\n$$\n\\begin{aligned}\nT_1&=W(q;0.025,0.215),\\quad T_2=W(q;0.57,0.82),\\\\\nm_r&=0.78T_1-0.52W(q;0.22,0.66).\n\\end{aligned}\n$$');
const geometry=m('6. Procedural jellyfish geometry').replaceAll('## 6.1','###').replaceAll('## 6.2','###');
const refs=sec(read('paper/pelagic.md'),'References').replace(/<div[^>]*>|<\/div>/g,'').replace(/<span[^>]*>|<\/span>/g,'');
const refEntries=sec(read('paper/pelagic.md'),'References').matchAll(/<div id="ref-([^"]+)"[^>]*>\n([\s\S]*?)\n<\/div>/g);
const referenceText=[...refEntries].map(([,id,body])=>`<a name="ref-${id}"></a>\n\n${body.trim().replace(/<\/?span[^>]*>/g,'')}`).join('\n\n');
const causal=`Malformed synthetic spatial event → non-finite pointer/current state → invalid transforms and dynamic geometry → tissue disappearance. The corrected boundary rejects non-finite spatial coordinates before they enter scene state. It does not sanitize final geometry, reset the ocean, or change tissue color. Natural idle lifecycle was not the identified root cause. This is the M6.6.1 robustness case, not a claim that every future context-loss failure has been solved.`;
const ablations=`These are development records, not controlled comparisons against other research systems. A historical before/after can change several mechanisms; it cannot establish an isolated speedup. The frozen optics diagnostic is separated from that history.\n\n| Attempt | Observed failure or boundary | Replacement / conclusion |\n| --- | --- | --- |\n| Over-directed M5 camera | Independent position, point-of-interest and safety corrections produced rejected janky travel | M5.2 bounded scalar progress and baked position/quaternion; no invented jerk-reduction percentage |\n| Giant dark analytic lens | Real bending worked, but the dark disc failed art review | Brief refractive hero-bubble passage with cheaper ambient films; analytic optics retained |\n| Bright/smooth sanctuary | Broad color and early slabs read as flat, bright or repetitive | Connected meso geology, dark mineral response and restrained highlights |\n| Repeated shelf primitive | Similar ledges remained obvious across close/medium views | Deterministic shelf-silhouette variation and shared batching, not additional rock count |\n| Separate idle canvas | It did not contain the current ocean color needed for refraction | Shared M7 scene target/output, with only small field-state ping-pong |\n| Idle-return geometry diagnosis | Malformed synthetic pointer coordinates were the earlier invalid state | Finite spatial-input boundary checks, not a final-geometry patch |\n| Asset2 / optional GTAO, review-only | Initial depth-fetch mismatch; corrected prototype returned identity AO and unhelpful depth | Not approved, not the README runtime, not a demonstrated AO improvement |\n\n${m('23. Ablations and rejected approaches').slice(m('23. Ablations and rejected approaches').indexOf('A connected-particulate'),m('23. Ablations and rejected approaches').indexOf('Rejected giant dark'))}\n\nThe later Asset2 experiment retained a material-space approximation rather than useful GTAO. Its measurements are not substituted for the approved-runtime results. The earlier implementation records are preserved as evidence, not examples to recreate for this README.`;
const perf=m('21. Experimental methodology').replaceAll('Fresh performance measurements','The reused publication measurements').replaceAll('fresh-results table','results table')+'\n\n'+m('22. Results').split('## 22.3 Fresh performance\n')[1].split('## 22.4 Compatibility scope')[0];
const validation=`The source, capture and test records below belong to the approved publication runtime. This README conversion reruns the necessary build/tests/parity and byte guard; it reuses the earlier recordings and capture-free measurements. Those are different validation events.\n\n${m('22. Results').split('## 22.1 Correctness and invariants\n')[1].split('## 22.2 Visual evidence')[0]}\n\n| Check | Evidence and scope |\n| --- | --- |\n| Full tests | 130 passing after the production build; no general npm test script is invented |\n| Approved motion parity | 720 frames / 24 exact checkpoints |\n| Default animal parity | 240 frames / eight geometry checkpoints and material response |\n| Runtime identity | 116 protected files compared byte-for-byte with approved source |\n| Native motion review | All 12 retained MP4s played to completion; no browser errors in the recorded gallery check |\n| Lifecycle footage | S8 entry, S9 contact, S10 pointer/recovery, S11 minute change, S12 pinch/exit |\n| Ownership | One main renderer/context and one ocean render in the active source; extra field/output draws are explicitly counted separately |\n| Responsive evidence | Desktop/narrow/portrait browser inspection, not physical-device validation |\n\nNo new 20-cycle or allocation-stability campaign was run for this documentation conversion. Earlier M7 reports contain lifecycle/resource tests on their named commits; they are not relabeled as fresh tests of another runtime. The current source retains bounded accumulators, persistent target ownership and serialized teardown. Those source contracts and the retained clips support narrower claims than a universal leak-free or crash-free guarantee.\n\n${m('22. Results').split('## 22.4 Compatibility scope\n')[1]}\n\nFour small inline GIFs make the methods visible without a separate reader site. Ten reused figures include generated diagrams, actual browser captures and a raw-data plot. The [capture catalog](paper/media/catalog.json) preserves renderer, viewport, drawing buffer, DPR, seed, phase and source timing; [README media provenance](docs/readme/media.json) records the exact excerpt and encoding. Original videos are approximately 30 observed browser frames/s, encoded at 30 fps with possible repetitions. The smaller GIFs are 10 fps publication previews, not a runtime frame-rate claim. Masters remain outside normal Git and no unissued remote media URL is invented.`;
const reproduce=`The executable baseline is **${runtime}**. These commands select that immutable runtime, not an unspecified main branch. The documentation branch is local until authorized; the public demo may still show a different review release.\n\n\`\`\`sh\ngit clone https://github.com/Denoax/pelagic-jellyfish-webgl.git\ncd pelagic-jellyfish-webgl\ngit switch --detach ${runtime}\nnpm ci\nVITE_OCEAN_RELEASE=milestone-2 npm run build\nnode --test tests/*.test.mjs\nnode scripts/check-approved-motion.mjs\nnode scripts/check-default-animal.mjs\nnpm run dev\n\`\`\`\n\nThe clone/switch sequence requires the named commit to be available from the remote. This local review does not claim it has been published there. In the existing full publication checkout, skip clone/switch and use its byte-identical runtime. Do not substitute a different remote commit if the requested object is absent. Historical parity scripts require their original Git objects; a history-free runtime snapshot can build but cannot manufacture those comparisons.\n\nBuild before tests: the Sites packaging test reads generated output. The historical release flag selects the approved production path; its name does not mean the implementation ends at M2. Vite prints the local development address. \`npm run preview\` serves the built artwork; no paper route or PDF builder is needed to understand or run the system.\n\n| Inspection | Query / control | Scope |\n| --- | --- | --- |\n| Explicit fallback | \`?renderer=webgl\` | Select WebGL2; verify the actual backend rather than trusting the renderer class name |\n| Keep ocean awake | \`?renderer=webgl&idle=300\` | Existing long idle-delay fixture |\n| Approved specimen | \`?renderer=webgl&specimen=1&animal=candidate&idle=300\` | DEV-only specimen, same approved animal; the historical candidate label is not Asset2 |\n| Camera modes | View → Documentary / Drift / Intimate / Deep / Explore | Existing world and accepted observer controls |\n| Normal liquid clock | Leave input inactive for 30 seconds | Reduced motion/readiness policy applies; pointer deforms, click/keyboard dismisses |\n\nNode 24.20.0 and the committed lockfile were used. Three.js is pinned to 0.175.0. Publication helper dependencies are separate from the application; no runtime package upgrade is needed. In the publication checkout, \`node scripts/publication/source-manifest.mjs\` verifies approved-file hashes.\n\nThe retained benchmark and motion tools accept an explicit local address and external evidence directory. They record scene state and timing separately; deterministic seeding does not make wall-clock browser scheduling bitwise repeatable. Never run recording/encoding concurrently with timing and report the result as capture-free. [REPRODUCIBILITY.md](REPRODUCIBILITY.md) retains optional tool details and archival paper-build instructions; this README already contains the complete method and runtime commands.`;
const frames=m('14. Shared optical compositor');
const contributions=os('Contributions');
const notationExtra={
 geometry:'Here the surface outputs and $R,H,y_r$ use animal-local world units. Latitude, contraction, lobe phase and local current controls are dimensionless/art-scaled inputs; azimuth is radians. In the membrane equation $b$ is the sideways offset and $f$ the fold offset in the transported section frame; its $t$ is normalized arm length, not seconds.',
 locomotion:'In equations (3)–(6), $T_1,T_2,refill,coast,drift,motionScale$ are dimensionless authored controls; $scale$ is animal scale. $X,V,e,C_{ij}$ are world position, velocity, route error and route correction, respectively; $h$ is seconds. The distance $d$ in pair separation is between desired positions. Neither that correction nor the soft route tether is a measured hydrodynamic force.',
 appendages:'All chain positions, rest lengths and named prediction increments are in the transported animal-local frame; damping $d_0$ and frame scale $\\sigma$ are dimensionless. Here $r_i$ denotes the pointer increment, whereas the scalar $r$ in the projection is rest length. The notation is local to these two expressions.',
 tissue:'Here $n_v$ and $v_{eye}$ are normalized view-space normal and eye direction. $d_t$ is dimensionless artistic optical thickness; $A$ is an absorption weight, $\\alpha$ the alpha coefficient, and $opacity$ a material control. The exponential is Beer–Lambert-inspired, not a calibrated spectral extinction law.',
 current:'Ambient $(x,y,z)$ and wake $X,X_e,r,R,R_0$ are world coordinates/lengths; $t,a,L$ are seconds, $\\hat b$ is a unit world axis, and $k,E$ are bounded authored weights. The field supplies artist-scaled world-unit velocity to its observers, not a conserved momentum density.',
 lod:'$R$ in the projection includes animal world scale; $z$ is positive view depth. $H,D$ are CSS pixels, $Z$ is dimensionless zoom and $f$ is degrees. $d,k$ are detail coordinates (far 0, medium 1, near 2); the transition time uses seconds. Fixed per-ID quality allocation would leave a large distant-route animal crude merely because of its ID. The adopted importance adapter avoids that failure without replacing either route source.',
 camera:'In equation (13), $s,e$ are dimensionless progress, $v$ progress/second, and $a_m$ progress/second². The baked polynomial uses segment-local $t\\in[0,1]$, not elapsed seconds; $p,\\Delta,m,n$ carry the units of the scalar being authored (world position or unwrapped angle). Playback samples $P(s)$ and $Q(s)$; it does not aim continuously at a moving animal.',
 bubble:'For equations (15)–(16), $o,d,r$ belong to lens-local ray coordinates, $A,B,C$ are quadratic coefficients and $t_e$ is the entry ray parameter. $E_v,D_o,z_s$ are metric view-space quantities; negative view Z points forward. $q$ is homogeneous clip position here, not pulse phase. $uv_r$ is dimensionless top-origin screen UV. The authored relative-index ratio enters the two refraction calls in the linked source.',
 sanctuary:'The plume uses world-space $X,V,U,U_f,V_d$; $h$ and particle age $a$ are seconds, while the numerical follow gains are artist-selected inverse-time scales. The thermal shimmer samples the shared live image, rather than making the particle field a fluid or temperature solver.',
 liquid:'For equations (18)–(21), $u,\\Delta,D$ are screen-UV coordinates/displacements with aspect correction where stated; $W,H$ are drawing-buffer pixels. $F,G,D_j,N_j,R_j,a,b,T,B,I$ are dimensionless fields or envelopes. $V,V_0,V_p$ are screen-space velocity controls, $h$ is seconds, and $i$ is the contact-site index. The entry-drop $t$ is local time after that site’s contact; its radius is in viewport-height units. These are optical/artistic units, not material mass or volume.',
};
let result=[start,
section('Abstract',m('Abstract')),
section('System at a glance',`| Layer | Implementation | Evidence |\n| --- | --- | --- |\n| Organism | Procedural bell/folded arms, pulse-coupled velocity, persistent constrained chains | Geometry and motion below |\n| Water/population | Bounded current/wakes, layered snow, projected-size detail | Field equations and LOD thresholds |\n| Observer | Accepted M5.2 bounded progress → baked position/quaternion; four tracks and Explore | Camera method below |\n| Optics | One clean live ocean color/depth source; world bubbles, thermal domains and screen-space liquid | Shared compositor and optical limits |\n| Runtime | Approved M7 ${runtime}; Three.js 0.175.0 | Immutable source links and byte guard |\n| Measurement | Actual NVIDIA WebGL2, 1280×900, DPR 1, bloom off | Seven retained capture-free runs |`,`**README.md is the publication.** No separate paper website is required. The live demo link points to the separately published artwork, not a promise that Pages currently serves this local publication baseline. Later Asset2 is unapproved and excluded from the final method and performance results.`),
section('Contributions',contributions),
section('Contents',headings.map(h=>`- [${h}](#${slug(h)})`).join('\n')),
section('Technical overview',overview,m('3. Related work and scope of influence')),
section('Mathematical notation',notation),
section(headings[2],geometry,notationExtra.geometry,source(['src/scene/anatomy/mantle.js','4-L54'])),
section(headings[3],bell,m('8. Locomotion and schooling'),notationExtra.locomotion,source(['src/scene/jellyMotion.js','10-L58'],['src/scene/JellySchoolDirector.js','209-L275'])),
section(headings[4],img('docs/readme/appendages.gif','Oblique approved specimen pulsing while folded oral arms and tentacles lag behind body motion; existing S2 recording.'),'*S2 excerpt, seven seconds at original speed. The change is remembered motion, not a freshly posed ribbon each frame.*',m('9. Stateful appendages'),notationExtra.appendages,source(['src/scene/LivingAppendages.js','888-L948'],['src/scene/anatomy/mantle.js','35-L54'])),
section(headings[5],m('10. Tissue optics and bioluminescence'),notationExtra.tissue,source(['src/scene/materials/JellyTissue.js','17-L39'])),
section(headings[6],m('11. Connected current and wake'),notationExtra.current,source(['src/scene/ocean/CurrentField.js','17-L79'],'src/scene/ocean/ConnectedOcean.js','src/scene/ocean/OceanSnow.js')),
section(headings[7],m('12. Projected-importance population detail'),notationExtra.lod,source(['src/scene/population/Importance.js','4-L30'],'src/scene/population/PopulationDetail.js','src/scene/population/PopulationAnimal.js')),
section(headings[8],m('13. Camera journey and exploration').replace('the paper does not','this method does not'),notationExtra.camera,source(['src/scene/camera/JourneyController.js','35-L58'],['src/scene/camera/CameraTrack.js','31-L47'])),
section(headings[9],img('docs/readme/refraction.gif','Moving jellyfish and particles optically displaced through live refractive bubbles in the existing S5 browser sequence.'),'*S5 excerpt: watch the image inside a passing hero bubble, not only its boundary. Cheap ambient films do not each run the analytic lookup.*',m('15. Bubble optics and lifetime'),notationExtra.bubble,'**World bubbles and idle liquid are different domains.** Bubbles use world-space analytic interfaces projected into the current ocean image. The clock is a screen-space implicit field. Both sample the clean live ocean, but neither reconstructs complete light transport.',source(['src/scene/glass/LiveOceanLens.js','171-L259'],'src/scene/glass/BubblePopulation.js')),
section(headings[10],m('16. Abyssal sanctuary and participating-media approximation'),notationExtra.sanctuary,source(['src/scene/sanctuary/VentDynamics.js','46-L69'],'src/scene/sanctuary/Sanctuary.js','src/scene/sanctuary/materials.js')),
section(headings[11],img('docs/readme/liquid.gif','Existing full-frame S9 recording: clock beads approach strokes, join through narrow necks and settle while the living ocean remains visible.'),'*S9 contact/merge at normal full-frame scale; a compressed preview of the existing master, not a new simulation or a zoomed diagnostic.*',m('17. Idle state and implicit liquid topology'),m('18. Persistent deformation and event choreography'),notationExtra.liquid,source(['src/scene/glass/OceanIdleGlass.js','114-L179'],['src/scene/glass/IdleDisplacement.js','22-L38'],['src/scene/glass/LiquidChoreography.js','40-L77'])),
section('Numerical robustness',causal,m('19. Lifecycle and timing'),'Here $A$ is accumulated seconds, $dt$ the admitted frame duration, $N$ the integer number of fixed steps and $h$ their duration. Fixed steps are per-subsystem policies: the whole application is not one globally bitwise-deterministic solve.',source(['src/scene/glass/IdleLiquidState.js','21-L31'],'src/components/HeroScene.jsx')),
section('Rendering architecture',graph,m('4. System overview').replace(/Figure 2 summarizes[\s\S]*$/,''),frames,m('20. Implementation and artifact boundary'),source('src/components/HeroScene.jsx','src/scene/glass/LiveOceanLens.js','src/scene/glass/OceanIdleGlass.js')),
section('Engineering ablations / failed approaches',ablations),
section('Performance',perf),section('Validation',validation),section('Reproducibility',reproduce),
section('Limitations',m('24. Limitations'),'The inherited clock can also appear stretched after a portrait resize; the retained inspection records show that limitation rather than retouching it. No runtime visual fix is part of this README conversion.'),
section('Technical stack',`| Component | Exact publication implementation |\n| --- | --- |\n| Application/lifecycle | React 19.2.0, imperative scene ownership |\n| Graphics | Three.js 0.175.0; WebGPURenderer with node/TSL materials |\n| Validated backend | NVIDIA ANGLE WebGL2; class name is not backend proof |\n| Build | Vite 6.4.2, existing Sites/static packaging |\n| Tool runtime | Node 24.20.0 with committed lockfile |\n| Evidence tooling | Browser CDP capture, FFmpeg derivatives, capture-free completion probe |\n| Original-source policy | MIT; third-party notices retained |`),
section('Repository map',`| Path | Responsibility |\n| --- | --- |\n| README.md | Complete technical publication |\n| src/components/HeroScene.jsx | Renderer and active scene ownership |\n| src/scene/anatomy/, LivingAppendages.js | Procedural surfaces and persistent tissue |\n| src/scene/ocean/ | Shared current, events and particulate |\n| src/scene/population/ | Screen-importance detail adapter |\n| src/scene/camera/ | Input-driven journey and observer tracks |\n| src/scene/glass/ | Shared optical output and live-ocean liquid |\n| src/scene/sanctuary/ | Approved deep geology and vent presentation |\n| docs/readme/ | Small derived GIFs, provenance and README validation |\n| paper/figures/, media/, results/ | Reused source-linked figures, capture metadata and raw measurements |\n| paper/source-map.md | Equation-to-immutable-source index |\n| paper/manuscript.md and supplement-source.md | Preserved prior prose cache/history, not a required reader destination |\n| scripts/publication/ | Reusable capture, timing and publication tools |\n| tests/ | Existing deterministic and packaging tests |`),
section('References',referenceText),
section('Citation','Mani Marami Milani is the author. This is an independent technical manuscript in local review, not a conference acceptance or peer-reviewed publication. No DOI, affiliation or release date is invented. The runtime commit identifies the executable experiment separately from this README-only publication commit.','```bibtex\n'+read('CITATION.bib').trim()+'\n```','[CITATION.cff](CITATION.cff) supports repository-aware citation tools; [CITATION.bib](CITATION.bib) contains the same bibliographic identity.'),
section('Licensing and asset provenance',m('26. AI assistance and provenance'),'See [LICENSE.md](LICENSE.md), the complete [MIT](LICENSES/MIT.txt) and [CC BY 4.0](LICENSES/CC-BY-4.0.txt) texts, [ASSET_PROVENANCE.md](ASSET_PROVENANCE.md), and [CONTRIBUTING.md](CONTRIBUTING.md). This documentation does not relicense third-party fonts, Aurelia code, scans or reference material. README preparation has not pushed, merged, deployed, created a release or modified Pages.')
].join('\n\n');
result=result.replace(/\[@([^\]]+)\]/g,(_,id)=>`[${refLabels[id]}](#ref-${id})`)
 .replaceAll('](figures/','](paper/figures/')
 .replaceAll('`results/benchmark.json`','[the raw benchmark](paper/results/benchmark.json)')
 .replaceAll('<!-- PUBLICATION_RESULTS -->',read('paper/results/table.md').trim())
 .replaceAll('The pinned WebGL multisample limitation is recorded in the supplement.','The pinned WebGL multisample limitation is described under Limitations below.')
 .replaceAll('The supplement records actual timing and warns against interpreting the authored neck as conserved fluid.','Actual timing is retained in the capture metadata; the authored neck is not conserved fluid.')
 .replaceAll('Normal-size merge and pinch\nfootage','Normal-size merge and pinch\nfootage');
// Figure identifiers stay tied to the existing cache; no redundant hero/system still.
// Convert all ten retained captions to sequential README numbers, including references.
const ids=[3,4,5,6,8,9,10,11,7,12];const numbers=new Map(ids.map((n,i)=>[n,i+1]));
result=result.replace(/Figure (\d+)/g,(s,n)=>numbers.has(+n)?'Figure '+numbers.get(+n):s);
result=result.replace(/\n{3,}/g,'\n\n').trim()+'\n';
result=result.replaceAll('src/components/HeroScene.jsx','src/scene/HeroScene.jsx').replaceAll('10 fps publication previews','8 fps publication previews');
if(result.includes('undefined')||result.includes('[@'))throw Error('Unresolved salvage token');
writeFileSync('README.md',result);
console.log('Salvaged README words:',result.split(/\s+/).length);
